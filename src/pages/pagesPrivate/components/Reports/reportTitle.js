export default function reportTitle(title) {
  return [
    {
      text: `Relatóio de ${title}`,
      fontSize: 16,
      bold: true,
      margin: [15, 20, 0, 45], // left, top, right, bottom
    },
  ];
}
